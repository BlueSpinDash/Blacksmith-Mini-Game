.shots/
node_modules/

# Android build output and local machine config
android/build/
android/app/build/
android/.gradle/
android/local.properties

# Signing material never belongs in version control
android/keystore.properties
*.jks
*.keystore

# Built apps and generated previews are distributed as releases, not committed
dist/
*.apk
*.aab
